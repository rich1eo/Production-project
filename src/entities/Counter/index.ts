import Counter from './ui/Counter';
import { counterReducer } from './model/slice/counterSlice';
import type { CounterShema } from './model/types/counterSchema';

export { Counter, counterReducer, CounterShema };
